import { IConstants, ILengthResponse } from '../types'

export const buildMessage = (lengthResponse: ILengthResponse, constants: IConstants) => lengthResponse.length === constants.DEFAULT_LENGTH && lengthResponse.isDefault ? `Your password with default length = ${lengthResponse.length} (as configured in constants.ts ${JSON.stringify(constants)}) is \n` : `Your password with length = ${lengthResponse.length} is \n`